/**
 * Placeholder for the blackjack domain engine (Phase 1).
 * Kept here so the Vitest setup and `src/blackjack` (pure TypeScript,
 * no framework imports) are exercised from Phase 0 onward.
 */
export function isEngineReady(): boolean {
  return true;
}
