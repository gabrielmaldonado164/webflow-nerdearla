# Phase 3 — Stats, Skill Map, adaptive practice, achievements

## Objective
Turn persisted decisions into visible progress: per-player stats, a per-category Skill Map, a "Practice weakness" mode that biases scenarios toward the weakest category, and achievements — all inside the game.

## Problem / why
Phase 2b stores every decision, but the player never sees long-term progress. The demo script needs "personalized stats" within 60 s, and Phase 4's coach tool `get_player_stats` needs a stats function.

## Scope
- In: pure stats + achievements derivation, `GET /api/stats`, adaptive scenario weighting + "Practice weakness", Skill Map panel opened from the HUD (pixel-art overlay over the table) with a short summary on the game-over overlay.
- Out: AI coach (Phase 4), daily challenge/leaderboard (Phase 5), separate `/stats` route (owner chose the HUD panel, 2026-09-23).

## Constraints
- Strategy and categories come from the engine (`src/blackjack`); never fork rules in the UI.
- Stats/achievements/weights are pure TypeScript, test-first, no new test deps; routes are thin adapters (handler pattern from `src/app/api/decisions/handler.ts`).
- `GET /api/stats` never mints a cookie; no/invalid cookie → empty stats `200`.
- The app must work when the API fails: the panel falls back to in-session stats (`src/features/practice/sessionStats.ts`).
- `runtime = "nodejs"`; client fetch prefixed with `NEXT_PUBLIC_BASE_PATH`.
- Mobile-first; respect `prefers-reduced-motion`; keyboard accessible panel (Esc closes, focus management).

## TDD
- Mode: Strict TDD, enabled (source: user global CLAUDE.md "Strict TDD Mode: enabled"; project CLAUDE.md "developed test-first").
- Runner: `npm test` (vitest, node environment, `src/**/*.test.ts`).

## Checks (every task)
`npm test`, `npx tsc --noEmit`, `npm run lint`, `npm run build`; UI task adds mobile + desktop screenshots.

## Tasks
- [x] T1 — Stats: pure `computePlayerStats(decisions)` (totals, accuracy, current and best streak, per-category totals/accuracy, strongest/weakest with a minimum sample), D1 query of a player's decisions, `GET /api/stats` via a pure tested handler.
- [x] T2 — Achievements: pure `deriveAchievements(decisions)` with a small fixed badge catalog (e.g. first perfect decision, 10 correct in a row, 10 soft hands in a row correct, never stood on 12 vs 2 across N such hands, every category attempted, 100 decisions); included in the `/api/stats` response.
- [x] T3 — Adaptive weighting: pure `weightsFromStats(stats)` (`weight = base + weaknessFactor`, bounded) feeding the existing scenario generator; `usePracticeSession` accepts weights and a "practice weakness" focus without breaking seeded determinism.
- [x] T4 — UI: HUD "Skill Map" button → pixel-art panel overlay (per-category bars, accuracy, streaks, badges, "Practice weakness" toggle), fetched from `/api/stats` and refreshed after decisions, falling back to session stats offline; short summary on the game-over overlay. Mobile/desktop screenshots.
- [x] T5 — T4 review follow-ups (keep last good stats, weights only on explicit toggle, shared category labels, sequenced refresh)

## Acceptance criteria
- A player with persisted decisions opens the Skill Map and sees accurate per-category progress and earned badges.
- "Practice weakness" measurably shifts scenarios toward the weakest category (tested on the pure weighting).
- With `/api/stats` failing, the panel still shows session stats and the game plays identically.

## Delivery
- Strategy: `ask-on-risk`; reusing the owner's standing **feature-branch-chain**. Stacked on `feat/2b-t3-client-wiring`: `feat/3-t1-stats` → `feat/3-t2-achievements` → `feat/3-t3-adaptive-weighting` → `feat/3-t4-skill-map-ui`.
- Forecast: ~900–1300 authored changed lines.
- Last reviewed boundary: 876b99d (T4 lineage review-627be78d2a226436, 4 lenses, approved + acknowledged; before: 1e585dc, lineage review-ef22fe2ab41deb2b approved + acknowledged, covering Phase 2b T5 and Phase 3 T1–T3 from 5ca76cf).

## Route per task
| Task | Route | Trigger evidence |
|---|---|---|
| T1–T3 | Delegated writer | 2+ non-trivial files per task |
| T4 | Delegated writer | 2+ non-trivial files |
| T5 | Delegated writer | 2+ non-trivial files |

## Progress
- 2026-09-23: Document created. Owner approved Phase 3 and chose the HUD panel for the Skill Map.
- 2026-09-23: T1 done on `feat/3-t1-stats` (commit c010c43). Strict TDD observed for all three new units (RED confirmed by temporarily removing the just-written implementation and re-running vitest before restoring it, then GREEN): `src/player/playerStats.ts` (`computePlayerStats`, 10 tests), `src/db/decisionsRepository.ts` additions (`decisionRowFromSelectValues` + `listPlayerDecisions`, 4 new tests), `src/app/api/stats/{handler,route}.ts` (`handleStatsRequest`, 6 tests). Checks: `npm test` 426/426 passed, `npx tsc --noEmit` clean, `npm run lint` clean, `npm run build` clean (`/api/stats` registered as a dynamic route). Local smoke test run: `npm run db:migrate:local` (no-op, already applied), `npm run cf:build` + `npx opennextjs-cloudflare preview -- --port 8799` (isolated from the pre-existing dev server on :3000, PID 51367, never touched), `GET /api/stats` with no cookie → 200 empty stats, `POST /api/decisions` x3 → cookie minted, `GET /api/stats` with that cookie → correct totals/per-category stats; preview processes killed afterward, `git status` clean of build artifacts. Assumptions: `MIN_ATTEMPTS_FOR_RANKING = 5` per the task's suggested constant, documented in `playerStats.ts`; a perfect (zero-miss) category is never reported as weakest (mirrors `sessionStats.ts`'s existing convention); strongest/weakest ties break on fixed category order (hard, soft, pair); GET /api/stats response body is `{ stats: PlayerStats }` (T2 will add a sibling `achievements` key, not nest under `stats`).

- 2026-09-23: T2 done on `feat/3-t2-achievements` (commit 913b609). Strict TDD observed throughout (RED confirmed for `deriveAchievements`, RED confirmed again for the `/api/stats` handler's `achievements` field wiring). On the first run against the real implementation, 16/17 `deriveAchievements` tests passed but one (`ten_soft_streak`, interior-miss case) failed on a wrong expected value in the test itself (expected progress 90, correct answer 50 per best-streak semantics); fixed the test assertion, re-ran, 17/17 green — reported honestly rather than as a first-try pass. `src/player/achievements.ts` (`deriveAchievements`, 17 tests) adds a fixed 6-badge catalog: `first_perfect_decision`, `ten_correct_streak`, `ten_soft_streak`, `never_stood_12_vs_2`, `every_category_attempted`, `hundred_decisions`. `GET /api/stats` now returns `{ stats, achievements }`. Checks: `npm test` 443/443 passed, `npx tsc --noEmit` clean, `npm run lint` clean, `npm run build` clean. Local smoke: `npm run cf:build` + preview on an isolated port (8799, pre-existing dev server on :3000/PID 51367 untouched); `GET /api/stats` confirmed to return the full achievements array alongside stats; preview processes killed afterward, `git status` clean. Assumptions: reused `computePlayerStats(...).bestStreak` directly for both the overall and the soft-filtered streak badges (no separate streak helper needed); `never_stood_12_vs_2` uses the row's stored `optimalAction === "hit"` (engine-decided at record time) plus `handValue(playerCards).total === 12` and `dealerUpcard.rank === "2"`, never assuming or re-deriving the strategy table; a single stand violation permanently zeroes that badge's progress for the given history (no partial credit); `every_category_attempted` reuses the newly-exported `CATEGORIES` constant from `playerStats.ts`; progress is always an integer 0-100, capped, with no `null` case. Unresolved: none for T2's stated scope.

- 2026-09-23: T3 done on `feat/3-t3-adaptive-weighting` (commit 7445ba7). Strict TDD observed for both testable units (RED confirmed for `weightsFromStats`, RED confirmed for the `dealNextHandIfAllowed` weighting/distribution-shift test). `src/training/weights.ts` (`weightsFromStats`, 9 tests): `weight = CATEGORY_WEIGHT_BASE(1) + weaknessFactor` (bounded by `MAX_WEAKNESS_FACTOR = 3`) once a category reaches `MIN_ATTEMPTS_FOR_RANKING` (reused from `playerStats.ts`), else a flat `LOW_SAMPLE_EXPLORATION_BOOST = 0.5`; `focusWeakness` multiplies only the weakest category's weight by `FOCUS_WEAKNESS_MULTIPLIER = 4`, leaving the others untouched (so never zeroed — every weight stays `>= 1`). `dealNextHandIfAllowed` (`sessionRng.ts`) takes optional `CategoryWeights`, forwarded to `generateScenario`; a seeded test (`sessionRng.test.ts`) shows a heavily pair-weighted draw shifting the category distribution well above the unweighted baseline (>80% pair share over 60 draws vs. the unweighted baseline), and a second test proves `undefined` weights are byte-for-byte identical to omitting the argument. `usePracticeSession` now takes an optional `weights` option (read once at mount into a ref) and returns `setWeights` for imperative changes — the exact seam T4 needs for a "Practice weakness" toggle; no UI built. Checks: `npm test` 454/454 passed (all 9 pre-existing seeded-determinism/practice tests still green, confirming backward compatibility), `npx tsc --noEmit` clean, `npm run lint` clean, `npm run build` clean. No local smoke test for T3 (task scoped smoke to T1/T2 only; T3 touches no persistence or API surface — verified instead by the seeded unit tests above). Assumptions: `MAX_WEAKNESS_FACTOR = 3`, `LOW_SAMPLE_EXPLORATION_BOOST = 0.5`, `FOCUS_WEAKNESS_MULTIPLIER = 4` are this task's own constants (not specified numerically in the brief), chosen so a 100%-error reliable category tops out at weight 4 (4x baseline) and `focusWeakness` pushes a weak category further (up to 16x baseline combined) while every other category always stays `>= 1`; `usePracticeSession` has no existing unit-test harness in this repo (no `@testing-library/react`/renderHook dependency, and adding one would violate "no new test deps"), so the hook-level wiring itself is verified by type-checking, the existing pixel-casino consumer continuing to build/lint clean, and full coverage at the pure-function layer below it (`weightsFromStats`, `dealNextHandIfAllowed`) — same convention this repo already uses for `route.ts` thin adapters. Unresolved: none for T3's stated scope; T4 will need to decide how "Practice weakness" turns on/off (e.g. call `setWeights(weightsFromStats(stats, { focusWeakness: true }))` vs `setWeights(undefined)`).

- 2026-09-23: Phase 2b T5 + Phase 3 T1–T3 review approved and acknowledged (lineage review-ef22fe2ab41deb2b, 5ca76cf..1e585dc, reliability lens). Follow-ups folded into T4 start: (WARNING) the engine-throw test in `src/app/api/decisions/handler.test.ts` depends on test order (call `vi.resetModules()` inside the test before `vi.doMock`); (SUGGESTION) `listPlayerDecisions` orders only by `createdAt`, so same-millisecond ties are non-deterministic (add a secondary sort key, e.g. `id`); (SUGGESTION) the `sessionRng` "identical to omitting weights" test is tautological (compare against `generateScenario(rng, rules)` with no options).

- 2026-09-23: Step 0 review follow-ups fixed, one commit each, before T4 UI work: `16e6665` (`src/app/api/decisions/handler.test.ts` — the engine-throw test's `vi.resetModules()` previously only ran in the file's `afterEach`, which never fires before the first test in a run; moved a `vi.resetModules()` call inside the test itself, right before `vi.doMock`. Confirmed RED standalone with `npx vitest run src/app/api/decisions/handler.test.ts -t "engine call throws"` before the fix — real engine ran instead of the mocked throw, `result.status` was `201` not `500` — and GREEN after), `3afd138` (`src/db/decisionsRepository.ts` — `listPlayerDecisions` now orders by `createdAt` then `id` ascending, breaking same-millisecond ties deterministically; no cheap unit test: the function calls Drizzle's D1 query builder directly and this repo has no fake/in-memory D1, and adding one (e.g. `better-sqlite3`) would be a new test dependency, which the task instructions rule out — documented as unresolved-by-design in the function's own doc comment, consistent with how the rest of this file's D1-touching functions are covered only by type-checking plus the pure encode/decode tests), `9b13cd9` (`src/features/practice/sessionRng.test.ts` — the "identical to omitting weights" test compared two `dealNextHandIfAllowed` calls against each other, which JS treats identically for an omitted vs. explicit-`undefined` optional argument, so it could never fail; rewrote it to compare against `generateScenario` called directly with no options object, actually exercising the engine's true default path).
- 2026-09-23: T4 done on `feat/3-t4-skill-map-ui` (commit `7112745`). All five new pure/testable units were TDD'd RED-then-GREEN (confirmed via `Cannot find module` failures before each implementation file existed, then full pass after): `skillMapStatsResponse.ts` (`parseStatsResponse`, 10 tests — validates the `GET /api/stats` response shape, rejecting the handler's own `{error}` 500 body, malformed category stats, bad achievement entries, etc.), `skillMapStatsRequest.ts` (`buildStatsRequestUrl`, 2 tests, mirrors `decisionRequest.ts`'s base-path pattern), `fetchSkillMapStats.ts` (`fetchSkillMapStats`, 6 tests — same-origin GET, non-2xx/unparsable/malformed/rejected/throwing fetch all resolve to `null`), `skillMapViewModel.ts` (`buildSkillMapViewModel`, 3 tests — unifies server `PlayerStats`/`Achievement[]` and the offline `SessionStats` fallback into one shape, offline gets `achievements: null` and `strongestCategory: null` since the session fallback has neither), `skillMapWeights.ts` (`computeToggleWeights`, 3 tests — bridges the toggle to `weightsFromStats`, `undefined` with no server data regardless of toggle state), `skillMapSummary.ts` (`summarizeForGameOver`, 5 tests — weakest category beats badge count beats a generic encouragement). `useSkillMapData.ts` (fetch + debounced refetch hook) is a thin, impure adapter, not unit-tested — no DOM/hook-testing harness in this repo and adding one would be a new test dependency, the same constraint T3 documented for `usePracticeSession`; covered by type-checking and the live browser verification below. UI: `SkillMapPanel.tsx`/`.module.css` (new), `Hud.tsx`/`.module.css` (added the "Skill Map" button), `PixelCasinoScreen.tsx` (wiring: `skillMapOpen`/`focusWeakness` state, `useSkillMapData`, the weights-applying effect, keyboard gating), `GameOverOverlay.tsx`/`.module.css` (added the `summary` line). Checks: `npm test` 483/483 passed, `npx tsc --noEmit` clean, `npm run lint` clean, `npm run build` clean. Live verification against a local D1 preview (`npm run db:migrate:local` no-op already applied, `npm run cf:build` + `opennextjs-cloudflare preview -- --port 8798`, isolated from the pre-existing dev server on :3000/PID 51367, never touched, killed after): played ~25 then ~90 real decisions via simulated keyboard input to populate server stats/badges/streaks across the same player cookie; opened the panel and confirmed real per-category bars, the strongest/weakest callout, 2 earned + 4 locked-with-progress badges, and the toggle (`aria-checked` flips on click); confirmed Esc closes the panel and returns focus to the HUD button (`document.activeElement` check); confirmed the table's `H` key is swallowed while the panel is open (decision count unchanged before/after a keydown); drove the run to game over and confirmed the overlay's summary line ("Weakest spot: Soft hands (30%) — keep practicing!"); monkey-patched `window.fetch` in-page to reject `/api/stats` and confirmed the panel switches to the offline note, session-derived bars, a disabled toggle with an explanatory note, and "Badges unavailable offline." while the game kept dealing/grading hands normally. Screenshots saved under the session scratchpad's `phase3/` directory: `desktop-skill-map-panel.png` (1280x800, panel open with real server data and 6 badges), `mobile-skill-map-panel.png` (390x844 via Playwright viewport emulation, no horizontal scroll, badge grid stacked to one column), `game-over-summary.png` (game-over overlay with the weakest-category summary line), `offline-fallback.png` (offline note, session-only bars, disabled toggle, hidden badges). Assumptions: the "Practice weakness" toggle calls `weightsFromStats(stats, {focusWeakness})` on every change (including from server-data updates while the panel is open) whenever server stats exist, even when off — matching the task's literal "off → adaptive weights without focus" wording, so opening the panel with existing data already biases scenario generation mildly even before the player touches the toggle; with no server data (offline, or a brand-new player) weights always resolve to `undefined` regardless of toggle position, and the toggle control itself is disabled offline with an explanatory note; the game-over summary prioritizes a named weakest category, then an earned-badge count, then a generic encouragement, and does not attempt to detect "a badge earned this specific run" (no before/after diffing) — the task's own wording ("e.g.") treats these as illustrative options, not a required combination; the offline session fallback has no `strongestCategory` (only `weakestCategory`, mirroring `sessionStats.ts`'s own narrower scope) and no achievements, so those render as absent/hidden rather than fabricated. Unresolved: `listPlayerDecisions`'s new ordering has no unit test (see Step 0 above) — same no-new-test-deps constraint; a full a11y focus-trap (Tab cycling inside the panel) was not implemented, only initial-focus-in and return-focus-on-close, since the task only asked for those two, not full trapping.

- 2026-09-24: T4 review (high risk, 4 lenses) approved and acknowledged (lineage review-627be78d2a226436, 1e585dc..876b99d). Non-blocking follow-ups: (WARNING) `useSkillMapData.refresh` replaces good server data with null on any failed refetch, flipping the panel offline and silently resetting weights while the Practice weakness toggle still shows on; (WARNING) adaptive weights start applying after the first game over even if the player never opens the Skill Map (skillMapActive is true when the run is over), and revert on a failed refetch; (SUGGESTION) three copies of the category order (one unused export) and a duplicated CATEGORY_LABEL map; (SUGGESTION) `GameOverOverlay` summary prop doc says null until loaded but it never is; (SUGGESTION) post-decision refetch is a fixed 500 ms debounce not sequenced after the POST, so stats can lag one decision. Process note: the T4 writer left a `wrangler dev` on port 8798 running; the parent stopped it.

- 2026-09-24: T5 (T4 review follow-ups) done on `feat/3-t4-skill-map-ui`, four commits, one per fix: `c2272c6` (WARNING — keep last good data: extracted the pure `nextSkillMapData` reducer, `src/features/pixel-casino/skillMapDataState.ts`, new file: RED confirmed by moving the just-written implementation out of the source tree and re-running `npx vitest run` against the new test file — `Cannot find module`, restored it, GREEN, 6/6 passed. `useSkillMapData` now dispatches every fetch result through it: success replaces `data`/clears `stale`; failure keeps the previous `data` reference and marks `stale`. The offline session fallback only kicks in while `data` is still `null`, i.e. before any fetch has ever succeeded), `67ee72a` (WARNING — weights only on explicit toggle, per the owner's 2026-09-24 decision: rewrote `skillMapWeights.test.ts` first — RED confirmed against the old implementation, `returns undefined ... whenever the toggle is off` failed with `{hard:1,soft:4,pair:1.6}` instead of `undefined` — then rewrote `computeToggleWeights` to check `focusWeakness` before `serverStats`, returning `undefined` whenever the toggle is off regardless of data, and `weightsFromStats(stats, {focusWeakness:true})` only when on with data; GREEN, 4/4. Updated `PixelCasinoScreen.tsx`'s weights effect comment to document the decoupling — fetching stats for the game-over summary no longer implicitly biases scenario generation, only flipping the toggle does — and fixed the misleading ROADMAP line), `1c6ea07` (SUGGESTION a/b, mechanical refactor, no behavior change so no RED/GREEN cycle applies — verified via the full suite staying green before/after: exported `CATEGORY_LABEL` alongside the existing `SKILL_MAP_CATEGORY_ORDER` from `skillMapViewModel.ts`; `PixelCasinoScreen`, `SkillMapPanel`, `skillMapSummary`, and `skillMapStatsResponse` now import both instead of keeping their own copies; `SkillMapPanel` maps `viewModel.categories` directly instead of a hardcoded array + `.find()`; fixed `GameOverOverlay`'s `summary` prop doc/type — `summarizeForGameOver` never returns `null`, so it's a required `string`, not `string | null`), `7a55a69` (SUGGESTION c — sequenced refresh: extended `sendDecision.test.ts` first with three new "resolves, never rejects" assertions — RED confirmed, `TypeError: You must provide a Promise to expect() when using .resolves, not 'undefined'` since the old `sendDecision` returned `void` — then changed `sendDecision` to return `Promise<void>` that always resolves once the POST settles (success, rejection, or a synchronous throw), never rejecting; GREEN, 7/7. `useSkillMapData` traded its `decisionsCount`-driven timer for an imperative `notifyDecisionSettled()` (still internally debounced, to coalesce a burst of near-simultaneous notifications) that `PixelCasinoScreen`'s new `handleDecision` calls from `sendDecision(record).then(...)`; `onDecision` stays fire-and-forget from `usePracticeSession`'s point of view — the game itself still never awaits it). Checks (run once after each commit): `npm test` climbed 483 → 489 → 490 → 490 → 493 passed, `npx tsc --noEmit` clean throughout, `npm run lint` clean throughout, `npm run build` clean (verified again after the T5c commit). Live verification against a local D1 preview (`npm run db:migrate:local` no-op, `npm run cf:build` + `opennextjs-cloudflare preview -- --port 8797`, isolated from the pre-existing dev server on :3000/PID 51367 — already exited on its own by the time this session started, never touched by this session either way; preview killed after, confirmed via `pgrep -fl "wrangler|workerd"` returning nothing): played 20 hands via simulated keyboard input against real persisted D1 stats, opened the Skill Map panel and confirmed the per-category rows/labels render correctly through the new shared `CATEGORY_LABEL`/`SKILL_MAP_CATEGORY_ORDER`, toggled Practice weakness on and off (`aria-checked` flips), then monkey-patched `window.fetch` to reject only `/api/stats` and played one more hand — confirmed the panel kept showing the last-good server data (accuracy, categories, strongest/weakest, full badge list, toggle still enabled) instead of falling back to the offline session view, proving fix 1 end-to-end. Screenshots saved under the session scratchpad's `phase3-fix/` directory: `desktop-skill-map-toggle-on.png`, `mobile-skill-map-toggle-off.png` (390x844), `desktop-kept-last-good-data-on-failed-refetch.png`. Assumptions: `nextSkillMapData`'s `stale` field is plumbed through `useSkillMapData`'s return type but not yet surfaced in the panel UI — the task asked to stop losing data, not to add a new "stats may be outdated" indicator, so that's left as a hook for a future task if wanted; `notifyDecisionSettled`'s debounce default stayed 500ms (same value T4 used for its blind timer) since the task said "keep a small debounce if needed to coalesce" without specifying a new number. Unresolved: none for T5's stated scope.

## Next step
Phase 3 (T1-T5) complete. Next: Phase 4 (AI coach) — not started this session.
